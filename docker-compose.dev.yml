services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: byhadmade
      POSTGRES_PASSWORD: byhadmade_secret
      POSTGRES_DB: byhadmade
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
